# AutoLyrics — Phase 1+ core stack
# Install PyTorch separately for your platform/CUDA (see scripts/check_environment.py)

# ASR & fine-tuning
transformers>=4.40.0
datasets>=2.19.0
peft>=0.11.0
accelerate>=0.30.0
openai-whisper>=20231117

# Audio
torchaudio>=2.2.0
soundfile>=0.12.1
librosa>=0.10.0

# Evaluation & demo
jiwer>=3.0.0
gradio>=4.0.0

# Utilities
numpy>=1.26.0
tqdm>=4.66.0
huggingface-hub>=0.23.0

# Optional: 4-bit LoRA (Linux/CUDA; often unavailable on Windows CPU)
# bitsandbytes>=0.43.0
